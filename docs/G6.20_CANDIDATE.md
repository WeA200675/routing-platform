# G6.20 concrete local LLM candidate

This document records a candidate for reproducible review. It is **not release approval** and does not bypass the existing manifest, SBOM, packaged-artifact or physical-device gates.

## Runtime candidate

- project: `ggml-org/llama.cpp`
- source: `https://github.com/ggml-org/llama.cpp`
- immutable revision: `b23701f77d47dad9de834d59ebfcbe25c9e8b46f`
- license to review: MIT
- Android build: source build with the repository-pinned NDK `28.2.13676358` and CMake `3.22.1`
- network/cloud dependency: none required for inference; model input is a verified local GGUF file

The revision above is deliberately a full Git commit SHA. It must not be replaced with `master`, `main`, a moving tag or `latest`.

## Model candidate

- family: SmolLM2 1.7B Instruct
- format: GGUF
- preferred quantization for first physical evaluation: Q4_K_M
- license to review: Apache-2.0
- expected size class: roughly 1.06 GB for Q4_K_M
- intended role: presentation/social response generation only

A specific GGUF repository revision and the exact GGUF SHA-256 are intentionally **not approved here**. Those values must be obtained from the exact downloaded candidate artifact and recorded immutably before the runtime can be admitted. A model card or repository name is not sufficient evidence.

## Required reproducible-candidate outputs

Before this candidate may replace the current no-LLM APK guard, produce and review together:

1. exact immutable model repository revision;
2. SHA-256 of the selected Q4_K_M GGUF;
3. SHA-256 of the runtime source archive at the pinned commit;
4. deterministic Android build arguments and one native artifact SHA-256 per admitted ABI;
5. SPDX 2.3 or CycloneDX 1.6 SBOM covering both runtime and model;
6. release manifest binding all of those identities;
7. independently hashed packaged APK native artifacts;
8. offline and physical-device evidence from that exact manifest.

## Authority boundary

The candidate does not receive route geometry mutation, positioning authority, maneuver truth, progress safety gates, permissions, emergency behavior, or critical-guidance authority. Those remain deterministic. Generated text remains presentation-only and passes through the existing deterministic enforcement/fallback boundary.

## Device gate

Do not merge merely because the candidate builds. After all software evidence is green, install the exact release candidate on the physical Pixel 10 Pro and capture the existing G6.20 latency, memory, offline, cancellation, lifecycle, safety and exact-artifact-identity evidence. Only a passing record for the exact release manifest can make the milestone merge-ready.
